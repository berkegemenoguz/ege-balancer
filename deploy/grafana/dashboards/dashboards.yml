apiVersion: 1

providers:
  - name: ege-balancer
    # The three dashboards link to each other; the folder keeps them together.
    folder: Ege-Balancer
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
